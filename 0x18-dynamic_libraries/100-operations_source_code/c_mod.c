#include "c_funcs.h"

/**
 * mod - find the rest of dividing a by b
 * @a: first number
 * @b: second number
 *
 * Return: the result of the modular operation
 */
int mod(int a, int b)
{
	return (a % b);
}
